machine:
  timezone:
    Australia/Brisbane
  node:
    version: 6.6.0
  environment:
    NODE_ENV: 'development'

general:
  branches:
    only:
      - master

database:
  override:
    - echo "No database"

dependencies:
  pre:
    - npm i yarn gulp-cli -g
  override:
    - yarn

deployment:
  production:
    branch: master
    commands:
      - yarn deploy
