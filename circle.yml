machine:
  timezone:
    Australia/Brisbane
  node:
    version: 6.8.1
  environment:
    NODE_ENV: 'development'

general:
  branches:
    only:
      - master

database:
  override:
    - echo "No database"

dependencies:
  pre:
    - npm i yarn gulp-cli -g
  override:
    - yarn install --pure-lockfile

deployment:
  production:
    branch: master
    commands:
      - yarn deploy
