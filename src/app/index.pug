extends ./_layout

mixin timeMix(format, time)
	time(datetime=moment.tz(time, LOC).format()) #{moment.tz(time, LOC).format(format)}

mixin rangeMix(range)
	span (#{moment.tz(range[0], LOC).to(moment.tz(range[1], LOC), true)})

mixin timeDif(range)
	+timeMix("MMMM YYYY", range[0])
	| -
	+timeMix("MMMM YYYY", range[1])
	+rangeMix(range)

block body

	.fold.fold--no-bg
		.fold__intro
			.fold__intro__wrapper
				h1 Marais
					span .
				p An experienced&nbsp;
					span.pop caffeine consumer
					| !
				blockquote {Try printing me}

	.fold
		.fold__content
			.sway
				ul
					li.sway--right
						figure
							figcaption
								h3 About
								p I'm Marais Rossouw, a creative and application programmer from Australia. With over 9 years experience in the design space from every technical angel

							.handsome
								img(src="img/handsome.jpg")

	.fold
		.fold__content
			.experience
				h2 Experience
				ul
					li.expereince__item
						article
							header
								h4 Wordpress Developer
								.expereince__time
									+timeDif(['2012-01-01', '2012-09-01'])
							main
								p.experience__descr Began my career as a PSD to Wordpress theme developer.

					li.expereince__item
						article
							header
								h4 Wordpress Developer
								.expereince__time
									+timeDif(['2012-01-01', '2012-09-01'])
							main
								p.experience__descr Began my career as a PSD to Wordpress theme developer.
